# ML and retrieval service

FastAPI service for AMU pattern anomaly inference and retrieval over approved project research/reference material. Train with `python training/train.py`; test with `pytest`. The bundled model uses **SYNTHETIC DEMONSTRATION DATA**, never regulatory data or claims about Indian farms. Express remains the authorized public API boundary.
